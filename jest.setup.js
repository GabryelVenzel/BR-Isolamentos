// Matchers extras (toBeInTheDocument, etc.) para quando os testes de
// componente forem escritos. Não é necessário para os testes de lib/ atuais,
// mas mantém o setup pronto.
import "@testing-library/jest-dom";
